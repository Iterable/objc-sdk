workspace 'Iterable-iOS-SDK.xcworkspace'
platform :ios, '7.0'

def shared_pods

end

target 'Iterable-iOS-SDK' do
    shared_pods
end

target 'Iterable-iOS-SDKTests' do
    shared_pods
    pod 'OHHTTPStubs'
end
